{
  "name": "crm-marmeria-server",
  "version": "2.3.0",
  "description": "Server centrale LAN per CRM Marmeria",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "node --check index.js && node --check app.js && node --check database.js && node --check restore-safety.js && node --check middleware/auth.js && node --check snapshot-compat.js && node --check smoke-test.js && node --check setup-test.js && node setup-test.js && node smoke-test.js create && node smoke-test.js update && node smoke-test.js conflict && node smoke-test.js integrity && node smoke-test.js snapshot && node smoke-test.js server"
  },
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "better-sqlite3": "^12.11.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.2.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
